<Window x:Class="ImageViewer.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageViewer"
        mc:Ignorable="d"
        Title="AboutWindow" Height="400" Width="640"
        Style="{DynamicResource CustomToolWindowStyle}"
        Foreground="{DynamicResource ControlDefaultForeground}">
    <Grid>
        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center" Margin="40" FontSize="24" Text="Image Viewer"/>
        <StackPanel Margin="80 100 80 80">
            <TextBlock HorizontalAlignment="Center" Margin="5" FontSize="16" Text="Main Info: This program is asynchronous, so it might take some time for images to load but they will load eventually"/>
            <TextBlock HorizontalAlignment="Center" Margin="5" FontSize="16" x:Name="description"/>
        </StackPanel>
    </Grid>
</Window>
